# Thư mục video minh hoạ

Các file video cho từng lệnh Grasshopper (nếu có) sẽ được đặt ở đây, cùng cấp với `assets/` (ảnh).
Khi tải toàn bộ repo về (GitHub → Code → Download ZIP), thư mục này và `assets/` đi kèm sẵn với `Grasshopper.html` nên mở offline không bị mất ảnh/video.
